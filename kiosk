#!/usr/bin/env bash
set -e
cd "$(dirname "$0")"

CMD="$1"; PROJ="$2"
if [ "$CMD" = "run" ] && [ -n "$PROJ" ]; then
  source .venv/bin/activate
  python "projects/$PROJ/run_bridge.py"
else
  echo "Usage: kiosk run <project-name>"
  echo "Example: kiosk run falklands"
fi
