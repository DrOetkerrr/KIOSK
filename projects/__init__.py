# projects/__init__.py
"""
Root package for all kiosk projects.

(Empty on purpose—just marks `projects/` as a Python package so
`python -m projects.falklands.run_bridge` will work once the rest
is in place.)
"""